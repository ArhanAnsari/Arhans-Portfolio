# AI Twin Configuration - Frontend Only (Recommended)
# Get your Google Gemini API Key from: https://aistudio.google.com/apikey

# REQUIRED: Google Gemini API Key for full AI Twin functionality
# Leave blank to use demo mode (predefined responses)
VITE_GEMINI_API_KEY=your_google_gemini_api_key_here

# Optional: Backend server URL (only if running backend separately)
# VITE_BACKEND_URL=http://localhost:3001

# Server Configuration (only needed if running backend)
PORT=3001
NODE_ENV=development

# Optional: Backend API Keys (only if using backend)
# ANTHROPIC_API_KEY=sk-ant-...your-key-here...
# GEMINI_API_KEY=your-gemini-key...

# Optional: For production deployments
# DATABASE_URL=...
# LOG_LEVEL=info
